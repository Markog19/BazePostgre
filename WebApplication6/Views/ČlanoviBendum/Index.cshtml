@model IEnumerable<WebApplication6.Models.ČlanoviBendum>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
    <form asp-controller="ČlanoviBendum" asp-action="CreateDocument" method="get">

        <div>



            <select name="vrsta">
                <option value="Instrument">Instrument</option>
                <option value="Band">Band</option>
                <option value="Glazbenik">Glazbenik</option>



            </select>



<p>
    <input type="text" name="filter">
    <input type="submit" value="Napravi PDF" />
</p>
            

    </div>
</form>
<form asp-controller="ČlanoviBendum" asp-action="CreateDocument" method="get">
    <input type="submit" value="Napravi PDF" />

</form>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Datum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Instrument)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Band)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Glazbenik)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Datum)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Instrument)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Band.Ime)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Glazbenik.Ime)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
